const donationSchema = require('./donation');
const newSchema = require('./new');
const userSchema = require('./user');
const kitSchema = require('./kit');
const donorSchema = require('./donor');

module.exports = {
    donationSchema,
    newSchema,
    userSchema,
    kitSchema,
    donorSchema
}